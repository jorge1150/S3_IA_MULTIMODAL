# ══════════════════════════════════════════════════════════════
# IA Multimodal - Soporte Técnico Computadoras Local — Requirements
# Probado en: macOS Intel x86_64 · Python 3.12
#
# IMPORTANTE: requiere Python 3.12 (torch 2.2 no tiene wheel para Py 3.13)
# Instalación automática: bash setup.sh
# Manual: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── PyTorch (CPU / MPS Apple Silicon) ────────────────────────
# 2.2.x: última versión con wheel para macOS Intel x86_64 y Python 3.12
torch==2.2.2
torchvision==0.17.2

# numpy<2: requerido — torch 2.2 fue compilado con NumPy 1.x
numpy<2

# ── STT — faster-whisper ──────────────────────────────────────
# Versión probada: 1.2.1 | cpu_threads=1 evita conflicto con torch en macOS
faster-whisper>=1.0.0

# ── Embeddings — OpenCLIP ViT-B-32 ───────────────────────────
# Versión probada: 3.3.0
open_clip_torch>=2.24.0

# ── Base vectorial local ──────────────────────────────────────
# Versión probada: 1.5.9
chromadb>=0.5.0

# ── Audio ─────────────────────────────────────────────────────
# PortAudio debe estar instalado: brew install portaudio
sounddevice>=0.4.6
scipy>=1.11.0

# ── Imagen / Video ────────────────────────────────────────────
Pillow>=10.0.0
# 4.8.x: última versión compatible con numpy<2
opencv-python==4.8.1.78

# ── TTS — Piper en español ────────────────────────────────────
# >=1.4.0: API con AudioChunk (sintetiza() retorna generador)
piper-tts>=1.4.0

# ── Interfaz web ──────────────────────────────────────────────
# Versión fijada: 6.16.0 (estable con Gradio 6.x API)
gradio==6.16.0

# ── HTTP / Ollama ─────────────────────────────────────────────
requests>=2.31.0

# ── Utilidades ────────────────────────────────────────────────
python-dotenv>=1.0.0

# ══════════════════════════════════════════════════════════════
# PASOS DE INSTALACIÓN (orden importante):
#
# 1. Crear entorno con Python 3.12:
#      /usr/local/bin/python3.12 -m venv venv
#      source venv/bin/activate
#
# 2. PortAudio (solo macOS, una sola vez):
#      brew install portaudio
#
# 3. Instalar dependencias:
#      pip install -r requirements.txt
#
# 4. Instalar Ollama y modelos LLM:
#      brew install ollama          (o desde https://ollama.com)
#      ollama serve                 ← en otra terminal
#      ollama pull tinyllama
#      ollama pull moondream
#
# 5. Descargar modelo Piper TTS en español:
#      python audio/download_piper.py
#
# 6. Construir base vectorial ChromaDB:
#      python rag/build_db.py
#
# 7. Lanzar sistema:
#      python app.py                ← http://localhost:7864
# ══════════════════════════════════════════════════════════════
